# ☯ 本源规划 · 插件本源 — 逆流解构与正本清源

> 反者道之动。前一轮认知又漂回"复杂大板全链路推演"——那是**后段**, 不是本源。
> 逆推全部提示词后, 本源锚定为: **把整套 KiCad 体系做成插件形态的 AI IDE 原生成员**,
> 前端对用户即 Copilot/Augment 式对话框, 底层全部替换为与 KiCad 的深度融合;
> 所有 VS Code 系 AI IDE (VS Code / Devin Desktop / Cursor / Windsurf / Cosa …) 一体适配。
> 大规模复杂 PCB 复刻是这套插件体系**打磨完成之后**的实践场, 不是当下重心。

## 一、提示词逆流解构 (隐藏在表层之下的驱动力)

| 表层描述 | 本源驱动力 |
|---|---|
| "做成插件的形式, 路由到用户的 VS Code 类插件" | 交付形态 = **插件**, 不是脚本/CLI 堆; 一切能力经插件面向用户 |
| "所有 VSCode 演化的 AI IDE 都能适配" | 插件只依赖标准 VS Code API + 本地 REST 桥, 零专有依赖 |
| "前端跟 AI copilot 差不多, 一个对话框" | UX 本源 = 对话框; 自然语言 → 意图 → 工具链, 用户不见底层 |
| "参照 Augment/Copilot 之类去推进优化" | 持续吸收成熟 AI IDE 的交互范式 (聊天面板/斜杠命令/上下文注入/内联结果) |
| "外接 API 两个方向: MD 文档 + 原生 API" | 双通道: Agent 读文档即接入; 第三方程序走原生 REST |
| "参照 devin remote 的 Proxiy Pro 插件设计" | 吸收: 渠道配置、模型路由、提示词隔离、面板化设计 |
| "集百家之长, 集大成者" | 开源/闭源工具皆可逆推, 提取哲学/模块折入本体 |
| "在实践中暴露缺陷, 当场完善, 持续 PR" | 演化机制 = 实践驱动 + 即时修复 + 自动合并 |

## 二、板块矩阵 — 成果 / 进度 / 未完成

### ✅ 已完成 (已合并 PR)

| 板块 | 内容 | PR |
|---|---|---|
| 桥闭环 | finisher 单调不劣化 + 布线预算按板规模伸缩 | #214 |
| 封装自愈 | 真实项目封装解析 + 本源规划文档 | #215 |
| 内层平面 | 4+ 层内层 GND/电源平面 + 逃逸过孔 | #216-217 |
| 内存护栏 | freerouting 堆上限 RAM/3 (≤2.5GiB), 免整机挂起 | #218 |
| **Copilot 对话** | `/api/agent` 意图路由 (中英正则, CJK 安全) + VS Code 聊天面板 (侧边栏+独立面板) + 慢动作 job 轮询 | #219 |
| **双通道外接** | AGENT_BRIDGE.md (`/api/doc` 机读) + 原生 REST 全量端点 | #219 |
| **Proxiy Pro 吸收** | `/api/channel` OpenAI 兼容渠道配置 (env 优先, `~/.dao/kicad_llm.json` 持久) + 自由问答 LLM 回退 | #219 |
| 真画板密度 | 去文字实体包围盒 + 尺寸自适应间距 (板面积 -37%) | #220 |
| 尾针收束器 | 过检间隙直线/L 弯缝合断尾, DRC 护栏单调不劣化 | #221 |

### 🔲 未完成 (按本源优先级排序)

| 优先 | 板块 | 需求 | 现状 |
|---|---|---|---|
| 1 | **对话面板深化** | 参照 Copilot/Augment: 会话历史持久、上下文注入 (当前工程/选中文件)、斜杠命令、流式输出、结果内联渲染 (板图 SVG 直出对话流) | 仅单回合 + 快捷 chip |
| 2 | **多 IDE 适配验证** | 在 Devin Desktop / Cursor / Windsurf 实机装载 vscode-dao-kicad, 修适配缺陷 | 仅 VS Code 本体验证 |
| 3 | **LLM 渠道深化** | 多轮上下文、工具调用式路由 (LLM 产出结构化意图而非仅兜底问答)、渠道面板 UI (吸收 Proxiy Pro 三面板) | 单回合兜底已通 |
| 4 | **原生 API 完备** | 鉴权 (token)、外部第三方接入示例、`/api/capabilities` 与文档同步校验 | 端点全量但无鉴权 |
| 5 | **KiCad 插件端** | pcbnew 内 ActionPlugin 面板与桥双向 (IDE ↔ KiCad GUI 实时联动) | 面板存在, 联动浅 |
| 6 | 布线收敛 | 剩余 unconnected (大间隙/跨层) 需过孔级缝合或迭代重布 | 充电板 0 violations / ~20 unconn |
| 7 | 连接度驱动布局深化 | 力导向嵌入已有, 需与自适应间距联调至手工密度 | 网格+聚簇可用 |
| 8 | 复杂大板复刻实践 | **后段**: 上述插件体系打牢后, 再回主板级全链路 | 暂停 (正确) |

## 三、恒定判据 (不变)

- 每个修复来自实践真实暴露的缺陷; 存在性门控; 闭环单调不劣化; 证据入文档。
- PR 持续推送, 自动合并, 不等待人工批准。

*道法自然 · 无为而无不为*
